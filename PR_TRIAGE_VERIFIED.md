# Verified PR Triage

Repository checked: `JosephSerUSP/Sillnight-Github` (the plan's `JosephSerUSP/Sillnight` slug is stale).

Every PR listed in `PR_TRIAGE_PLAN.md` was fetched through the GitHub PR-diff endpoint equivalent to `gh pr diff <n>` and read. Because the plan labels several lists as representative, I also diff-read title-matched open PRs found in the live repository search; those are called out below rather than silently treated as covered. PRs #224 and #225 returned empty current diffs; they are recorded as redundant only on that basis. `PR_TRIAGE_PLAN.md`'s one-off list duplicated #128, which is assigned only to the Fog-of-War analysis below.

| cluster | survivor PR | PRs to close | unique-work exceptions | confidence |
|---|---|---|---|---|
| Documentation drift audit | #287 | #218, #220, #222, #224, #225, #231, #267, #269, #271, #277, #279 | #176 rewrites the design document and refactor status; #211 adds `AUDIT_REPORT.md`; #232 documents TransitionManager effects, stat multipliers, and element-cycle behavior; #233 adds `DOCS_AUDIT.md`; #236 adds distinct UI/battle-pipeline documentation; #238 adds transition, state-manager, and animation details; #240 adds EffectRegistry/status documentation and code comments; #242 adds `AUDIT_REPORT.md` plus TransitionManager coverage; #246 adds distinct architecture/JSDoc corrections; #260 changes the documented Battle3D coupling; #263 documents animation-step callbacks and adds source JSDoc; #265 adds parameter getters and implementation-gap notes; #275 documents the animation-step contract; #281 adds `DRIFT_REPORT.md`; #283 adds a separate drift report; #285 documents formula, element, and animation-step details. These are promoted rather than blanket-closed. | High. #287 is the newest and most focused four-file audit, covering the recurring drift notes and inline JSDoc corrections. It is based directly on current `main` (`base_sha` `1e14a672...`) and is reported mergeable, so it applies cleanly. |
| AudioService / audio system | #286 | #266, #270, #276 | #235 couples audio to a broad reactive UI/event refactor; #262 is a different `AudioSystem` implementation with battle-observer BGM/SFX hooks; #264 adds the older `AudioSystem`, BattleObserver hooks, modal changes, and verification artifacts; #268 deletes/reworks verification scripts while implementing a different event mapping; #272 adds scene BGM hooks, asset directories, and BattleObserver integration; #282 also changes data-driven element logic, party UI, documentation, and verification; #284 adds a distinct drift report and different AudioService API. | High. #286 is the newest audio-only implementation with Config registration, service registration, EventBus battle/UI subscriptions, and `Window_Selectable` sound hooks. It is based directly on current `main` and reported mergeable; the promoted exceptions need separate review because they contain non-audio work or incompatible audio APIs. |
| Architecture assessment / rewrite proposals | None (split; no single survivor) | None | #147, #150, #157, #159, #160, #161, and #162 are separate assessment/proposal documents with materially different recommendations: registry/strategy, component-based rewrite, RPG Maker MVC, scene/spriteset lifecycle, and model/view battle separation. Title-matched #156 is a formula-parser/DI implementation and #158 is a registry extraction, not a duplicate proposal; both remain separate work. They are not redundant patches, so none should be closed as duplicates without an explicit product decision. | High. The diffs confirm distinct files and incompatible architectural directions rather than repeated implementation. No survivor is forced because completeness and applicability cannot be compared as if these were one change. |
| Fog of War wall displacement | None (split; no single survivor) | None | #122 replaces the shader with persistent visibility texture logic; #123 implements a different persistent fog texture/material path; #126 is random vertex warp; #128 is fog-target interpolation; #132 fixes the logical reveal radius; #134 fixes WebGL2 vertex texture sampling; #135 adds RGBA texture storage and view-space displacement. #128 is moved here from the plan's one-off row. | High. These are complementary or competing layers of the fix, not redundant copies; #122 and #123 also disagree on the material/texture approach, while #135 does not contain #128's interpolation or #132's reveal-radius correction. #134 is mergeable but #135 is not, and no one PR applies as a complete current-main survivor. |
| Battle transitions / shatter effect | #208 for the shatter subgroup; #201 and #203 remain separate | Conditional: #206 only after #208 is rebased/ported and validated | #201 is a DOM/CSS "Woosh" transition with camera intro and map wipe; #203 is a custom-shader swirl/cut-in/wipe system. #206 and #208 are the overlapping shatter implementations, but they use materially different mesh generation and shader-buffer layouts. | Medium. #208 is the later and broader shatter diff, but GitHub reports it non-mergeable against current `main`; therefore #206 should not be closed until the shatter behavior is successfully ported and revalidated. |
| Window/party layout | None (material disagreement) | None | #107 is documentation-only and should be promoted out of the implementation cluster; #110 uses responsive `1fr` rows; #111 uses fixed `110px` rows to prevent jitter; #237 and #239 add different reactive party-component/gauge architectures; #254 removes the Summoner column and changes party-menu navigation; #280 changes party UI coupling while also implementing `asp` and missing params. Both #110 and #111 also change HUD/party component rendering, so the row-sizing choice is a real product decision rather than redundant work. | High. The diffs show a genuine layout-policy conflict plus distinct component and menu designs, and both #110 and #111 are non-mergeable against current `main`. Retain them for an explicit responsive-vs-fixed and party-UI decision instead of selecting a false survivor. |
| Reactive variables/switches | #278 for the narrow backend/turn-order subgroup | #274 | #226, #227, and #228 repeat the event emission but also remove legacy action lookup (with #226 adding a data-integrity fixture); #229 and #230 preserve that lookup with different warning/fallback behavior; #234 repeats the backend event work; #237 and #239 add distinct reactive UI components; #241 adds dynamic actor effects/element state; #254 changes party-menu structure; #280 adds `asp`, missing params, and UI decoupling. These title-matched PRs make the overall representative cluster heterogeneous, so they are not blanket-close recommendations. | Medium. #278 contains #274's variable/switch events and adds Action Speed sorting, AI extraction, and Summoner-last behavior, and it is based directly on current `main` and reported mergeable. That is a defensible survivor only for the narrow backend/turn-order subgroup; the broader reactive/UI work has no single clean survivor. |
| Genuine one-offs | None (individual review) | None | #105 recruit crash; #120 recruit plus treasure-finding/animation work; #165 camera easing and Summoner positioning; #204 shop visibility; #216 EXP curve/gauge logic; #223 verification-fixture correction; #245 Summoner-last and missing-parameter implementation; #257 animation pacing race fix; #288 standalone visual data editor. These are independent features/bug fixes, not a redundancy cluster. | High. The diffs have different files, behavior, and acceptance criteria, so no one can represent the others. No close recommendation is made; #128 is deliberately analyzed under Fog of War instead. |

## Diff-read register

- Documentation drift: #176, #211, #218, #220, #222, #224, #225, #231, #232, #233, #236, #238, #240, #242, #246, #260, #263, #265, #267, #269, #271, #275, #277, #279, #281, #283, #285, #287.
- Audio system: #235, #262, #264, #266, #268, #270, #272, #276, #282, #284, #286.
- Architecture proposals/implementations: #147, #150, #156, #157, #158, #159, #160, #161, #162.
- Fog of War: #122, #123, #126, #128, #132, #134, #135.
- Battle transitions: #201, #203, #206, #208.
- Window/party layout: #107, #110, #111, #237, #239, #254, #280.
- Reactive state: #226, #227, #228, #229, #230, #234, #237, #239, #241, #254, #274, #278, #280.
- One-offs: #105, #120, #165, #204, #216, #223, #245, #257, #288.

## Cleanup result

The selected work was ported to current `main` as focused commits:

- `f711b10` — documentation drift and implementation-gap notes.
- `08739bd` — event-driven `AudioService` integration.
- `42b8674` — reactive variable/switch events, action-speed turn ordering, and enemy action extraction.
- `44a3cd6` — `BattleRenderSystem` ownership JSDoc.
- `36be5cc` — README, roadmap, and triage-record alignment.
- `9f2ac17` — ignore local agent settings.

All open PRs were closed with replacement-commit or future-Issue references. Future directions are tracked in Issues #289–#304. Remote branches were deleted only where their contents were verified as merged or fully represented by the selected commits/issues; unresolved alternatives remain retained until their contents receive branch-specific review.
